
# Requirements
```bash
sudo apt install qemu-kvm libvirt-daemon-system libvirt-clients bridge-utils cloud-image-utils ansible

ansible-galaxy install -r collections/requirements.yml


ansible-playbook -i inventory.ini playbook.yml


```
