# {{ ansible_managed }}
# PBX (Asterisk/FreePBX) port forwarding rules

pbx_ip = "192.168.3.3"

# SIP signaling
rdr pass on vtnet1 proto udp from any to $ext_ip port 5060 -> $pbx_ip port 5060

# RTP media ports
rdr pass on vtnet1 proto udp from any to $ext_ip port 10000:11000 -> $pbx_ip
