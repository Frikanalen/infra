$TTL {{ dns_soa_minimum }}
@   IN SOA  ns1.staging.frikanalen.no. hostmaster.staging.frikanalen.no. (
        {{ dns_soa_serial }} ; serial
        {{ dns_soa_refresh }} ; refresh
        {{ dns_soa_retry }}   ; retry
        {{ dns_soa_expire }}  ; expire
        {{ dns_soa_minimum }} ; minimum
)

    IN NS   ns1.staging.frikanalen.no.

; Name server A record
ns1 IN A {{ dns_ns1_public_address }}

; Add your records below
