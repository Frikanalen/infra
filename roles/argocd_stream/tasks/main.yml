---
- name: Deploy Stream via ArgoCD Application
  become: true
  kubernetes.core.k8s:
    definition: "{{ lookup('template', 'application.yml.j2') }}"
    state: present
    kubeconfig: "{{ k8s_kubeconfig }}"
