# Mount file01's media archive read-only; see roles/nfs_client and
# roles/nfs_server, and group_vars/storage.yml for the export side.
nfs_client_mounts:
  - src: "file01:/archive/media"
    path: /mnt/media
